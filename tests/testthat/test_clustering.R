context("Test clustering.")

# test_that("Test whether clustering() returns a list on the epi test data.", {
#   expect_is(clustering(df = epi$data,
#                        label = epi$label$label), "list")
# })
